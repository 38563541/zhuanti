### set up ros in tracer
#### downlaod dependencies
```
sudo apt install ros-noetic-teleop-twist-keyboard
sudo apt install ros-noetic-joint-state-publisher-gui
sudo apt install ros-noetic-ros-controllers
```
